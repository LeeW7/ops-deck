project:
  type: flutter
  name: Ops Deck

preview:
  enabled: true
  auto_deploy: true
  platform: firebase

  firebase:
    groups:
      - testers

tests:
  required: true
  command: "flutter test"
